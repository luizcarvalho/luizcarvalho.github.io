#!/bin/bash
git push origin `git subtree split --prefix _site code 2> /dev/null`:master --force


# echo $DEPLOYING
# if [ $DEPLOYING != 'yes' ]; then
#   echo 'Deploying site...'
#   export DEPLOYING='yes'
#   git push origin `git subtree split --prefix _site code 2> /dev/null`:master --force
#   export DEPLOYING='no'
#   echo 'done!'
# fi
# exit 0
