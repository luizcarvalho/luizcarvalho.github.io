#!/bin/bash
git push origin `git subtree split --prefix _site code 2> /dev/null`:master --force


# if [ DEPLOYING != 1 ]; then
#   echo 'Deploying site...'
#   export DEPLOYING = 1
#   git push origin `git subtree split --prefix _site code 2> /dev/null`:master --force
#   export DEPLOYING = 0
#   echo 'done!'
# fi
# exit 0
